/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
}

// next.config.js
module.exports = {
    output: 'export',
    images: {
      disableStaticImages: true,
    },
    // Other configurations...
  };
module.exports = nextConfig